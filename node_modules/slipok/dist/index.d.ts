import { SlipCheckQuotaResponse, SlipCheckResponse } from "./types";
export declare class Client {
    #private;
    constructor(branchId: string, apiKey: string);
    private request;
    checkSlip(data: string | Uint8Array, log?: boolean, amount?: number): Promise<SlipCheckResponse>;
    checkQuota(): Promise<SlipCheckQuotaResponse>;
}
export { BankCode, BankAccount, SlipCheckRequest, SlipCheckResponse, SlipCheckQuotaResponse, SLIP_CHECK_ERROR_CODE, } from "./types";
